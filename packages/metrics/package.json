{
  "name": "@preflight/metrics",
  "version": "0.0.0",
  "private": true,
  "description": "Turning a simulated launch into the numbers that answer whether a curve is any good.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@preflight/agents": "workspace:*",
    "@preflight/config": "workspace:*",
    "@preflight/core": "workspace:*"
  }
}

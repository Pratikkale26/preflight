# secrets — never commit
.env
.env.*
!.env.example
*.keypair.json
id.json

# internal planning; not part of the published project
notes/

# dependencies & build output
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# test & coverage artifacts
coverage/
.vitest/

# dumped on-chain program bytecode (fetched by script, not committed)
*.so

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
